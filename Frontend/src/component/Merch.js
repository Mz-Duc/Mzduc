import React, { Component } from 'react';

class Merch extends Component {
    render() {
        return (
            <div>
                day la merch
            </div>
        );
    }
}

export default Merch;